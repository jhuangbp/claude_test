# Railway 配置文件
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "datasette serve example.db --host 0.0.0.0 --port 8001 --cors --metadata metadata.yml --config-file datasette.yml"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
